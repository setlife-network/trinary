import React, { useState } from 'react';

const TimeEntryTile = (props) => {

    return (
        <div className='TimeEntryTile'>
            TimeEntryTile
        </div>
    );
}

TimeEntryTile.defaultProps = {
    
};

export default TimeEntryTile;
